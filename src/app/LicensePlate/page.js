import LicensePlate from '@/components/template/LicensePlate';
import React from 'react';

const Page = () => {
    return (
        <div>
            <LicensePlate/>
        </div>
    );
}

export default Page;
